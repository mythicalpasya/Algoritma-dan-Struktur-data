n = int(input("Masukkan nilai: "))
prima = []

for angka in range(2, n + 1):
    is_prima = True
    for i in range(2, angka):
        if angka % i == 0:
            is_prima = False
            break
    if is_prima:
        prima.append(angka)

print("Bilangan prima hingga n adalah", prima)
